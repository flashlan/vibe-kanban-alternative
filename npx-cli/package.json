{
  "name": "vibe-kanban-alternative",
  "private": false,
  "version": "0.2.40",
  "main": "index.js",
  "bin": {
    "vibe-kanban-alternative": "bin/cli.js",
    "vibe-kanban": "bin/cli.js",
    "vibe-kanban-indie": "bin/cli.js"
  },
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --target=node20 --format=cjs --outfile=bin/cli.js --external:adm-zip --banner:js=\"#!/usr/bin/env node\"",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "keywords": [],
  "author": "dexloom",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flashlan/vibe-kanban-alternative.git"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "license": "Apache-2.0",
  "description": "NPX wrapper around vibe-kanban-alternative — the single-developer, self-hosted fork of vibe-kanban with a TUI cockpit and Telegram channel orchestration",
  "devDependencies": {
    "esbuild": "^0.27.2"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "cac": "^7.0.0"
  },
  "files": [
    "bin"
  ]
}
